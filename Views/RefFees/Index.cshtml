@model IEnumerable<TMP2019.Models.DataModels.AccountingModels.DataModels.RefFee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FeeCategory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GameCategory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FeeHD)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FeeLD)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FeeCategory.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GameCategory.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FeeHD)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FeeLD)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
